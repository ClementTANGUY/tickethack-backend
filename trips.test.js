const request = require('supertest');
const app = require('./app');

// it('Get /trips/:departure', async () => {
//     const res = await request(app).get('/trips/departure/paris');
//     expect(res.statusCode).toBe(200);
//     expect(res.body.data).toEqual([]);
// });


